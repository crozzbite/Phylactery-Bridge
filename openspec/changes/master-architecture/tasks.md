# Mastery Tasks (Bridge)

- [ ] Master Identity Module <!-- id: 0 -->
    - [ ] Create `RegisterDto`, `LoginDto` with validation
    - [ ] Add Swagger decorators to `AuthController`
- [ ] Master Audit Module <!-- id: 1 -->
    - [ ] Create `StartAuditDto` (Class-based)
    - [ ] Update `AuditController` to use strict DTO
    - [ ] Add Swagger decorators
- [ ] Global Config <!-- id: 2 -->
    - [ ] Enable `ValidationPipe` globally in `main.ts`
    - [ ] Configure `DocumentBuilder` for Swagger in `main.ts`
